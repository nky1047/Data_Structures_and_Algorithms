package Algorithms;

public class Basic {





    /**
     * Used to solve problems such as
     * -Reverse an Array, -Two sum in sorted array
     * -closest two sum, -three sum, -four sum
     * -trapping rainwater
     * @return
     */
    public static int TwoPointer_Array(){
       /* initialize left=0 , right =n-1;
       Run a Loop while (left<right ) to traverse from both corners
       [ compute target ]
       [ after compute, increament left, decreament right -- untill left=right]
       [if before left==right, target is found -- success , else NOT FOUND]
*/
        return -1;
    }

}
